export const redisConfigs = {
  TTL: 60, // keeping it 60 seconds for testing purposes
  MAX: 10,
  keys: {
    SALES_REPORT: 'salesReport',
  }
};

export const reportsConfig = {
  topSellingItemsCount: 10
}
